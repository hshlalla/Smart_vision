## Smart Vision Demo - example environment
#
# Copy to `.env` and adjust for your environment.

# Milvus connection (docker-compose publishes 19530 on localhost by default)
MILVUS_URI=tcp://localhost:19530

# Gradio port
PORT=7860

# Local model directory (optional)
MODEL_DIR=models/
