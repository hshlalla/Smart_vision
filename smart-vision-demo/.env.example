## Smart Vision Demo - example environment
#
# Copy to `.env` and adjust for your environment.

# Milvus connection (docker-compose publishes 19530 on localhost by default)
MILVUS_URI=tcp://localhost:19530

# Gradio port
PORT=7860

# Local model directory (optional)
MODEL_DIR=models/

# (Optional) Caption generation is very slow on CPU. Default is enabled only when CUDA is available.
# Set to true to force-enable captioning on CPU.
ENABLE_CAPTIONER=true

# Captioner backend:
# - gpt/openai: use OpenAI vision models (recommended for CPU-only machines)
# - qwen: use local Qwen-VL (recommended only with GPU)
# - none/off: disable captioning
CAPTIONER_BACKEND=gpt

# OpenAI API (required when CAPTIONER_BACKEND=gpt)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
OPENAI_IMAGE_DETAIL=low
